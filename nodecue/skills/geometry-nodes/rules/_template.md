---
title: Category Title Here
section: category-id
description: "One-line description of this node family and when to use it."
tags: tag1, tag2
---

## Category Title

Reference nodes for `Category`. Total: **N** nodes.

### Node Name — `bl_idname`
- **Notes:** What this node does, key behavior, gotchas. Keep concise.
- **Inputs:**
  - `Socket Name` (`TYPE`) — typical upstream source or usage hint
- **Outputs:**
  - `Socket Name` (`TYPE`) — what this produces and where it typically goes
- **Properties** (if any writable properties beyond sockets):
  - `property_name`: allowed values or enum options
- **Example:**
  ```
  Upstream.Output → This Node.Input → Downstream.Input
  ```
